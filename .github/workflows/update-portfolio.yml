name: Update portfolio data

on:
  schedule:
    # Twice daily, Mon-Fri.
    # 17:00 UTC = midday ET.
    # 20:00 UTC = near U.S. market close.
    - cron: '0 17 * * 1-5'
    - cron: '0 20 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-portfolio
  cancel-in-progress: false

jobs:
  update-portfolio:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate portfolio.json
        run: python generate_portfolio_data.py

      - name: Validate output
        run: |
          python <<'PY'
          import json, sys
          d = json.load(open('portfolio.json'))
          n = len(d.get('holdings', []))
          print(f"holdings={n}")
          if n < 20:
              msg = f"Only {n} holdings fetched. Refusing to commit."
              print(f"::error::{msg}")
              sys.exit(1)
          PY

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          email="41898282+github-actions[bot]@users.noreply.github.com"
          git config user.email "$email"
          git add portfolio.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          stamp=$(date -u +"%Y-%m-%d %H:%MZ")
          git commit -m "data: portfolio update ${stamp}"
          # Integrate any commits pushed to the remote since checkout, then push.
          # portfolio.json is regenerated each run, so on conflict our version wins.
          git pull --rebase -X theirs
          git push
